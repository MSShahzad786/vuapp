Implement a feature where the UI displays subjects that the user has selected or not selected, based on data retrieved from the local storage table named 'selected_subject'.

Also remove refresh button. and it relivent functions. 

but you have to try to remove from firestore when user remove subject.